import styled from 'styled-components';

import { FormGroup } from '../../../styles';

export const CustomFormGroup = styled(FormGroup)`
  &#customerCarFormGroup {
    width: 78%;
  }

  &#dateFormGroup {
    width: 20%;
  }
`;
